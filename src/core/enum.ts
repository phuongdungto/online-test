export enum Roles { ADMIN = 'admin', TEACHER = 'teacher', STUDENT = 'student' };

export enum TypeTest { LISTENING = 'listening', WRITTING = 'writing', READING = 'reading' };


export enum TaskStatus { LOW = 0, MEDIUM = 1, HIGH = 2 }